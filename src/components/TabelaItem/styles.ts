import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: 40,
        borderBottomWidth: 1,
        flexDirection: 'row'
    },
    divisors:{
        justifyContent: 'center',
        paddingHorizontal: 3,
        borderRightWidth: 1
    },
    txtDivisors:{
        color: '#000',
        fontWeight: 'bold',
        fontSize: 11
    }
});